<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:components="clr-namespace:StudyMateProject.Components"
             xmlns:viewmodels="clr-namespace:StudyMateProject.ViewModels"
             x:Class="StudyMateProject.Views.CalculatorPage"
             x:DataType="viewmodels:CalculatorViewModel"
             Title="Калькулятор"
             Shell.NavBarIsVisible="True">

    <!-- Полноэкранная страница калькулятора -->
    <Grid RowDefinitions="Auto,*,Auto">

        <!-- Верхняя панель с переключателями режимов -->
        <Border Grid.Row="0" 
                BackgroundColor="{DynamicResource CalculatorHeaderBackgroundColor}"
                Padding="16,8">

            <Grid ColumnDefinitions="*,Auto,Auto">

                <!-- Переключатель вкладок калькулятора -->
                <ScrollView Grid.Column="0" Orientation="Horizontal">
                    <StackLayout Orientation="Horizontal" Spacing="8">

                        <Button Text="Обычный" 
                                Command="{Binding SwitchToBasicCommand}"
                                Style="{DynamicResource CalculatorTabButtonStyle}"
                                IsEnabled="{Binding CurrentCalculatorType, 
                                           Converter={StaticResource EnumToBoolConverter}, 
                                           ConverterParameter=Basic}"/>

                        <Button Text="Научный" 
                                Command="{Binding SwitchToScientificCommand}"
                                Style="{DynamicResource CalculatorTabButtonStyle}"/>

                        <Button Text="Матрицы" 
                                Command="{Binding SwitchToMatrixCommand}"
                                Style="{DynamicResource CalculatorTabButtonStyle}"/>

                        <Button Text="Продвинутый" 
                                Command="{Binding SwitchToAdvancedCommand}"
                                Style="{DynamicResource CalculatorTabButtonStyle}"/>

                    </StackLayout>
                </ScrollView>

                <!-- Кнопка настроек -->
                <Button Grid.Column="1"
                        Text="⚙️"
                        Command="{Binding ShowSettingsCommand}"
                        Style="{DynamicResource CalculatorHeaderButtonStyle}"
                        Margin="8,0"/>

                <!-- Кнопка истории -->
                <Button Grid.Column="2"
                        Text="{Binding ShowHistory, 
                               Converter={StaticResource BoolToHistoryIconConverter}}"
                        Command="{Binding ToggleHistoryCommand}"
                        Style="{DynamicResource CalculatorHeaderButtonStyle}"/>

            </Grid>
        </Border>

        <!-- Основной контент с калькулятором -->
        <Grid Grid.Row="1" ColumnDefinitions="*,Auto">

            <!-- Основной калькулятор -->
            <components:CalculatorComponent Grid.Column="0"
                                          BindingContext="{Binding}"
                                          IsMiniMode="False"
                                          FontSize="32"/>

            <!-- Боковая панель истории -->
            <Border Grid.Column="1"
                    IsVisible="{Binding ShowHistory}"
                    BackgroundColor="{DynamicResource CalculatorHistoryBackgroundColor}"
                    Stroke="{DynamicResource CalculatorHistoryBorderColor}"
                    StrokeThickness="1"
                    WidthRequest="300"
                    Margin="8,0,0,0">

                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="12"/>
                </Border.StrokeShape>

                <Grid RowDefinitions="Auto,*,Auto" Padding="12">

                    <!-- Заголовок истории -->
                    <Label Grid.Row="0"
                           Text="История вычислений"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource CalculatorHistoryTitleColor}"
                           Margin="0,0,0,8"/>

                    <!-- Список истории -->
                    <CollectionView Grid.Row="1"
                                   ItemsSource="{Binding HistoryItems}"
                                   SelectionMode="Single"
                                   SelectedItem="{Binding SelectedHistoryItem}">

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid RowDefinitions="Auto,Auto" 
                                      Padding="8,4"
                                      BackgroundColor="Transparent">

                                    <!-- Время -->
                                    <Label Grid.Row="0"
                                           Text="{Binding Timestamp, StringFormat='{0:HH:mm:ss}'}"
                                           FontSize="10"
                                           TextColor="{DynamicResource CalculatorHistoryTimeColor}"/>

                                    <!-- Выражение и результат -->
                                    <Label Grid.Row="1"
                                           FontSize="12"
                                           TextColor="{DynamicResource CalculatorHistoryTextColor}">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding Expression}" 
                                                      TextColor="{DynamicResource CalculatorHistoryExpressionColor}"/>
                                                <Span Text=" = "/>
                                                <Span Text="{Binding FormattedResult}" 
                                                      FontAttributes="Bold"
                                                      TextColor="{DynamicResource CalculatorHistoryResultColor}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <!-- Жест для повторного использования -->
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:CalculatorViewModel}}, Path=InsertFromHistoryCommand}"
                                                             CommandParameter="{Binding}"/>
                                    </Grid.GestureRecognizers>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!-- Кнопки управления историей -->
                    <StackLayout Grid.Row="2" 
                                Orientation="Horizontal" 
                                HorizontalOptions="Center"
                                Spacing="8"
                                Margin="0,8,0,0">

                        <Button Text="Очистить"
                                Command="{Binding ClearHistoryCommand}"
                                Style="{DynamicResource CalculatorHistoryButtonStyle}"/>

                        <Button Text="Экспорт"
                                Command="{Binding ExportHistoryCommand}"
                                Style="{DynamicResource CalculatorHistoryButtonStyle}"/>

                    </StackLayout>

                </Grid>
            </Border>

        </Grid>

        <!-- Нижняя панель с дополнительной информацией -->
        <Border Grid.Row="2"
                BackgroundColor="{DynamicResource CalculatorFooterBackgroundColor}"
                Padding="16,8"
                IsVisible="{Binding ShowStatusBar}">

            <Grid ColumnDefinitions="*,Auto,Auto">

                <!-- Режим углов -->
                <Label Grid.Column="0"
                       Text="{Binding AngleMode, StringFormat='Режим: {0}'}"
                       FontSize="12"
                       TextColor="{DynamicResource CalculatorFooterTextColor}"
                       VerticalOptions="Center"/>

                <!-- Индикатор вычислений -->
                <StackLayout Grid.Column="1" 
                            Orientation="Horizontal" 
                            Spacing="4"
                            IsVisible="{Binding IsCalculating}">
                    <ActivityIndicator IsRunning="{Binding IsCalculating}"
                                      Color="{DynamicResource Primary}"
                                      HeightRequest="16"/>
                    <Label Text="Вычисляется..."
                           FontSize="12"
                           TextColor="{DynamicResource CalculatorFooterTextColor}"
                           VerticalOptions="Center"/>
                </StackLayout>

                <!-- Кнопка копирования результата -->
                <Button Grid.Column="2"
                        Text="📋"
                        Command="{Binding CopyResultCommand}"
                        Style="{DynamicResource CalculatorFooterButtonStyle}"
                        ToolTipProperties.Text="Скопировать результат"/>

            </Grid>
        </Border>

    </Grid>
</ContentPage>